# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in ping-m.gemspec
gemspec

gem 'bundler'
gem 'minitest'
gem 'pry'
gem 'pry-rescue'
gem 'rake'
gem 'rubocop'
gem 'rubocop-minitest'
gem 'rubocop-rake'
gem 'simplecov'
